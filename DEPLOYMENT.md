# Deployment ledger — ep_native_RN (Pulse app)

**Rule:** any session that publishes an OTA or submits a build updates this table.
Publish ONLY via `npm run ota:production|preview|development` (see CLAUDE.md).
Check current published state with `eas update:list` — never trust this table alone
before publishing.

> Seeded 18 Aug 2026 — ⚠ entries need verification.

| Workstream | State | Notes | Last touched |
|---|---|---|---|
| `fix/analytics-groupids` branch | committed locally, NOT merged, NOT published | Rescued 18 Aug from the archived `ep_native_RN-main` duplicate (orig. 16 Jun): Amplitude was receiving `[undefined,…]` groupIds. Review → merge → include in next OTA. | 18 Aug |
| EP-1069 groups.ts revert | ⚠ local-only, NOT deployed | Held to avoid prod OTA footgun while v1 backend fix is held. | ⚠ |
| EP-1194 gitignore hardening + ASC key config | committed to `main` 13 Aug | | 13 Aug |
| **v3.2.0 store release** | **LIVE on both stores** — iOS approved + Android promoted to production, published manually by Maurice from the store consoles | On-device Time-Sensitive verification still open (see runbook) | 18 Aug |
| **EP-1193 Global Pulse coordinate mapping** | **PUBLISHED to `production` OTA 31 Aug** | Global Pulse hand-rolled the coordinate→grid mapping (`axis + 4`) instead of using the shared `useCoordinateMapping`; axis values have no zero and Y is inverted, so all 15 coordinates on the x=4 / y=4 bands fell off the 8x8 grid and the other 49 rendered mis-placed. Native-only — WeWeb/Teams reads the same 24h aggregate and was unaffected. Update group `7146cf16-2b71-41d8-a441-2538355f6d97`, runtime **3.2.0**, commit `b327cf9`, iOS + Android. | 31 Aug |
| **EP-1261 WhatsApp 2FA delivery option** | **PUBLISHED to `production` OTA 9 Sep** | Users outside AU/US/UK frequently never receive the SMS 2FA code since the international A2P/OTP regulation changes — and because the code is the second factor, they cannot log in at all. Adds a secondary "Send my code on WhatsApp instead" action to both screens that send a phone code: `MobileVerifyScreen` (login) and onboarding `PhoneVerificationStep`. `src/api/auth.ts` gains `DeliveryMethod`/`PhoneDeliveryMethod` mirroring the Xano enum; `signInWithMobile` only puts `delivery_method` on the wire when supplied, so the default login path is byte-identical. **`generateCode` no longer sends the legacy `type` (email\|phone)** — it and `delivery_method` described the same dimension, so the channel is now the only parameter (Maurice's call, 9 Sep). The backend still accepts `type` for callers not yet drained (older RN bundles, WeWeb); do NOT remove it server-side until those are migrated. **Design notes:** each channel has its OWN cooldown — a single shared timer would grey out WhatsApp for 30s on arrival, locking out exactly the users whose SMS never lands; and failure copy never reveals whether a number is registered on WhatsApp (anyone can type a phone number into that screen). Both endpoints answer HTTP 200 even when the send fails, carrying the provider status in the body, so the screens check `status !== 201` rather than relying on a thrown error. **Backend pairing:** `emotional-pulse` EP-1261 promoted to **v1** 9 Sep (`send_2fa_whatsapp` NEW + `send_2fa_phone` dispatcher + `delivery_method` on `generateCode`/`signinwithmobile`) — see that repo's ledger; verified end-to-end on staging (code delivered on WhatsApp, then `verifyMobileCode` returned `verified:true`). Shipped in TWO updates: group `08c78311-fdbf-4d62-83c5-518402c67e34` (commit `992a1bd`) put the option on the code-entry screens; group **`7cfab29b-57a8-4bee-961b-672e280c22c3`** (commit `90cadb5`) is the current one and moves the choice to where the number is ENTERED — `MobileSignInScreen` ("Continue" / "Continue with WhatsApp") and onboarding `PhoneEntryStep` ("Send Code" / "Send Code on WhatsApp"), matching the WeWeb flow (Maurice, 9 Sep). Offering WhatsApp first is not a loosening: SMS pumping pays attackers a share of premium SMS termination fees, WhatsApp authentication messages carry no such revenue share, so shifting legitimate traffic onto it REDUCES exposure on the abused channel — and both channels stay behind the same caps. The chosen channel is carried into the verify screen (new optional `deliveryMethod` route param on `MobileVerify` / `initialChannel` prop) so its copy and per-channel cooldowns describe what was actually sent. **Also fixed a pre-existing bug this surfaced:** `MobileSignInScreen` navigated to the code screen even when the send had FAILED, because the endpoint answers HTTP 200 with the provider status in the body — an unknown number landed the user on a code screen with no code coming. Now checks `status !== 201`. Runtime **3.2.0**, iOS + Android. **⚠ Not yet verified on a device through the app UI** — only through the HTTP endpoint. **⚠ Copy needs Steven's sign-off.** **⚠ WhatsApp shows the sender as the phone number, not "ShareTree"** — display-name review is separate from Meta business verification (which is complete); also the display name is "ShareTree" not "Emotional Pulse", changeable only once per 30 days via a Twilio support ticket. | 9 Sep |
| Last OTA publish | **9 Sep 2026** — EP-1261, group `7cfab29b-57a8-4bee-961b-672e280c22c3`, commit `90cadb5`, runtime 3.2.0 | ⚠ Runtime 3.2.0 — users who have NOT taken the v3.2.0 store update do not receive this update. That is a real gap for EP-1261 specifically, since the intended audience is international users who may well be on an older build: they stay locked out of login until they update from the store. Consider whether a store release is needed to reach them. | 9 Sep |
